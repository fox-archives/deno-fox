scripts:
  start:
    cmd: deno run --unstable cli.ts
    env:
      DENO_ENV: development
  watch:
    cmd: denon --config .denonrc.json run cli.ts
    log: info
  test:
    env:
      DENO_ENV: test
allow:
  - read
  - write
  - env
