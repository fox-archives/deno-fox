import { Application } from "https://deno.land/x/abc/mod.ts";<% if (isTs) { %><%= nl %>import type { Context } from "https://deno.land/x/abc/context.ts";<% } %>

const app = new Application();

app
  .get("/hello", (c<% if (isTs) { %>: Context<% } %>) => {
    return "Hello, Abc!";
  })

export { app }
